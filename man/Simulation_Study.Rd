% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{Simulation_Study}
\alias{Simulation_Study}
\title{Run the Full Monte Carlo Simulation Study}
\usage{
Simulation_Study(
  B = 500,
  n = 200,
  m_choices = c(4, 5, 6),
  n_test = 2000,
  p_grid = c(200, 500, 1000, 2000),
  tau_grid = c(0.25, 0.5, 0.75),
  scenarios = c("S1", "S2", "S3", "S4", "S5", "S6"),
  s_grid = c(10, 20, 40),
  b0_grid = c(0.5, 0.75),
  rho_grid = c(0.3, 0.7),
  sigma_gamma2_grid = c(0.25, 1, 4),
  delta_grid = c(0.3, 0.5),
  full_factorial = FALSE,
  methods = c("JP-DME-QR", "Naive QR-Lasso", "Two-step LQMM", "GLMM-Lasso",
    "Bayesian Mixed-Lasso", "Oracle QR"),
  c_screen = 3,
  T_iter = 2,
  h_mult = 1,
  lambda1_mult = 0.3,
  lambda_clime_mult = 1,
  inference_mode = c("auto", "CLIME", "RIDGE"),
  jp_target = c("beta_star_ref", "beta0"),
  n_ref_pseudotrue = 400,
  b_ref_pseudotrue = 3,
  seed = 2026,
  verbose = TRUE
)
}
\arguments{
\item{B}{Number of replications per configuration.}

\item{n}{Number of clusters in each replication.}

\item{m_choices}{Candidate cluster sizes.}

\item{n_test}{Test-set size for prediction-risk evaluation.}

\item{p_grid}{Grid of fixed-effect dimensions.}

\item{tau_grid}{Grid of quantile levels.}

\item{scenarios}{Error scenarios.}

\item{s_grid, b0_grid, rho_grid, sigma_gamma2_grid, delta_grid}{Scenario parameters.}

\item{full_factorial}{Logical; whether to use full factorial design.}

\item{methods}{Methods to benchmark.}

\item{c_screen}{Screening scaling constant.}

\item{T_iter}{Number of screening iterations.}

\item{h_mult, lambda1_mult, lambda_clime_mult}{Multipliers for tuning levels.}

\item{inference_mode}{Inference backend for \code{JP-DME-QR}.}

\item{jp_target}{Target used for JP-DME-QR evaluation.}

\item{n_ref_pseudotrue, b_ref_pseudotrue}{Settings for pseudo-true reference.}

\item{seed}{Base random seed.}

\item{verbose}{Logical; print progress if \code{TRUE}.}
}
\value{
A list with \code{config_grid}, \code{raw_results}, and \code{summary}.
}
\description{
Executes the manuscript simulation design across scenario/\code{p}/\code{tau} grids
and returns both replication-level and aggregated outputs.
}
\examples{
\dontrun{
sim <- Simulation_Study(
  B = 1,
  n = 20,
  m_choices = c(3, 4),
  n_test = 200,
  p_grid = c(30),
  tau_grid = c(0.5),
  scenarios = c("S1"),
  methods = c("JP-DME-QR", "Naive QR-Lasso"),
  inference_mode = "RIDGE",
  jp_target = "beta0",
  verbose = FALSE
)
names(sim)
}
}
