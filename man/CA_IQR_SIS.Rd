% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{CA_IQR_SIS}
\alias{CA_IQR_SIS}
\title{Cluster-Adjusted IQR-SIS Screening}
\usage{
CA_IQR_SIS(
  y,
  X,
  cluster_id,
  tau_grid = c(0.25, 0.5, 0.75),
  tau_target = 0.5,
  d_target,
  T_iter = 2,
  cumulative_targets = NULL,
  verbose = FALSE
)
}
\arguments{
\item{y}{Numeric response vector of length \code{N}.}

\item{X}{Numeric design matrix of dimension \verb{N x p}.}

\item{cluster_id}{Cluster labels of length \code{N}.}

\item{tau_grid}{Quantile levels used to build marginal screening scores.}

\item{tau_target}{Target quantile used in the residual update step.}

\item{d_target}{Target screening size.}

\item{T_iter}{Number of iterative screening rounds.}

\item{cumulative_targets}{Optional cumulative screening sizes per round.}

\item{verbose}{Logical; print progress if \code{TRUE}.}
}
\value{
Integer vector of selected variable indices.
}
\description{
Iterative screening for clustered quantile-regression data using within-cluster
median centering and marginal multi-quantile scores.
}
\examples{
set.seed(1)
dat <- generate_mixed_qr_data(
  n = 10, p = 12, s = 3, tau = 0.5,
  scenario = "S1", m_choices = c(2, 3)
)
active <- CA_IQR_SIS(
  y = dat$y,
  X = dat$X,
  cluster_id = dat$cluster_id,
  d_target = 5,
  T_iter = 1
)
active
}
